{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "format": "bunx @biomejs/biome format --write .",
    "lint": "bunx @biomejs/biome lint . --config-path=../biome.json",
    "type-check": "tsc --project tsconfig.json --pretty --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@playwright/test": "1.57.0",
    "@types/node": "22.19.2",
    "dotenv": "17.2.3",
    "typescript": "5.9.3"
  }
}
